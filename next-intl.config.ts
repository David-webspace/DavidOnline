import { IntlConfig } from 'next-intl';

const config = {
  locales: ['zh', 'en'], // Correctly define the supported locales
  defaultLocale: 'zh',
  localeDetection: false
};

export default config;
